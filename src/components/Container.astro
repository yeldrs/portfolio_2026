---
// File: src/components/Container.astro
export interface Props {
  /** Allows adding additional classes if needed. */
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`max-w-7xl mx-auto px-md md:px-xl ${className}`}>
  <slot />
</div>
